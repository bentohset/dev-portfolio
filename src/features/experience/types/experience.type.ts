export interface ExperienceType {
  role: string;
  company: string;
  logo: string;
  period: string;
  concise: string; 
  points: string[];
  skills: string[];
}

export interface AchievementType {
  title: string;
  award: string;
  period: string;
  image: string;
  desc: string;
}